[project]
name = "bryx-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.112.0",
    "bleak>=3.0.2",
    "dotenv>=0.9.9",
    "prompt-toolkit>=3.0.52",
]
